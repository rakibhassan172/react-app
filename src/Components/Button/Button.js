import React from "react";

const Button = () => {
  return (
    <div>
      <button type="button" className="blue-button">
        More Details
      </button>
    </div>
  );
};

export default Button;
